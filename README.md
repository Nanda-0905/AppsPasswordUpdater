# AppsPasswordUpdater

Windows application to update the password

1. Create a stand alone application that needs to run in the server and get all the webconfigs
2. Find and replace the String/UserID/Password
3. Create a backup file in case of a rollback
4. Commit the changes to the webconfig file and write the logs.
   w
